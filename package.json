{
  "name": "brainbank-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install-all": "npm install",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "echo 'No backend build needed'",
    "build": "npm run build --workspace=frontend"
  }
}
